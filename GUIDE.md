# 📘 Учебник по автоматизации на Java

Этот файл поможет тебе понять каждую строчку в нашем проекте.

---

## 1. Базовые слова Java (Словарь)

| Слово | Что это значит? | Простыми словами |
| :--- | :--- | :--- |
| **class** | Чертеж / Шаблон | Описание того, как выглядит и что умеет объект (например, Страница). |
| **public** | Публичный | Этот код виден всем остальным файлам. |
| **private** | Приватный | Скрытая информация, видна только внутри этого файла (как пароль). |
| **void** | Пустота | Метод просто делает работу и ничего не выдает в ответ. |
| **extends** | Расширяет / Наследует | Позволяет одному файлу забрать все умения другого (например, `LoginTest` забирает умения `BaseTest`). |
| **new** | Создай! | Команда создать реальный живой объект по чертежу (`class`). |
| **String** | Строка | Обычный текст в кавычках: `"привет"`. |

---

## 2. Магия Аннотаций (Символ @)

Аннотации — это команды для **JUnit** и **Allure**. Они говорят программе, как относиться к коду ниже.

- **`@Test`**: "Это не просто код, это ТЕСТ. Запусти его!".
- **`@BeforeEach`**: "Сделай это ПЕРЕД каждым тестом" (например, открой браузер).
- **`@AfterEach`**: "Сделай это ПОСЛЕ каждого теста" (например, закрой браузер).
- **`@DisplayName`**: "В отчете назови этот тест вот так (по-русски)".
- **`@Step`**: "Запиши это действие как отдельный шаг в красивый отчет Allure".

---

## 3. Как общаются файлы (Иерархия)

В нашем проекте строгий порядок:

1. **`src/main/java` (Инструкции)**
   - Здесь лежат **Page Objects** (например, `LoginPage.java`).
   - Они знают: "Где кнопка" и "Как на неё нажать".
   - Они НЕ знают: "Правильный это тест или нет".

2. **`src/test/java` (Приказы)**
   - Здесь лежат сами **Tests** (например, `LoginTest.java`).
   - Они знают: "Что мы проверяем".
   - Они используют **Page Objects**, чтобы отдавать команды браузеру.

3. **`BaseTest.java` (Фундамент)**
   - Главный родитель. Он умеет заводить мотор (браузер) и глушить его. Все тесты "стоят" на нем.

---

## 4. Разбор ключевой строки: `loginPage = new LoginPage(driver);`

Это самый важный момент. Что здесь происходит?

1. **`LoginPage`** (слева) — это тип нашего "помощника".
2. **`loginPage`** (в центре) — это имя, которое мы дали нашему помощнику.
3. **`new LoginPage(driver)`** (справа) — мы рождаем помощника и даем ему в руки **driver** (пульт управления браузером).

**Теперь наш помощник `loginPage` готов выполнять команды типа `.login()`, `.open()` и так далее.**

---

## 5. Как Selenium ищет элементы (Locators)

Команда `By.id("user-name")` — это как GPS-координаты:
- **`By`**: "Ищи с помощью..."
- **`.id`**: "...уникального номера (ID)"
- **`("user-name")`**: "...который называется 'user-name'".

После того как нашли, мы делаем действие:
- **`.sendKeys("текст")`** — напечатать.
- **`.click()`** — нажать.
- **`.getText()`** — прочитать, что написано.
